server:
  port: 8083

spring:
  application:
    name: INVENTORY-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/inventory_db
    username: root
    password: root@39

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
